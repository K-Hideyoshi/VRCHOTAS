<Window x:Class="VRCHOTAS.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Application Logs"
        Height="500"
        Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Margin="0,0,0,8"
                       Foreground="DimGray"
                       Text="Only the newest 100 log files are retained automatically in the log folder." />

            <DockPanel Grid.Row="1" LastChildFill="False" VerticalAlignment="Center">
                <TextBlock Text="Level filter:" VerticalAlignment="Center" Margin="0,0,8,0" DockPanel.Dock="Left" />
                <ItemsControl ItemsSource="{Binding LogLevelFilters}" DockPanel.Dock="Left" VerticalAlignment="Center">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel VerticalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Margin="0,0,12,0"
                                      VerticalAlignment="Center"
                                      Content="{Binding DisplayName}"
                                      IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Margin="12,0,0,0"
                        Padding="12,4"
                        VerticalAlignment="Center"
                        Content="Locate Current Log File"
                        Command="{Binding OpenCurrentLogFileLocationCommand}"
                        ToolTip="{Binding CurrentLogFilePath}"
                        DockPanel.Dock="Right" />
            </DockPanel>
        </Grid>

        <DataGrid x:Name="LogEntriesGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding FilteredLogs}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss.fff}" Width="190" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="90" />
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="150" />
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
